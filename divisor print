#include<stdio.h>
#include<time.h>
#include<stdlib.h>
#pragma warning(disable:4996)

int main()
{ 
  /*int num, i=1;
	printf("정수를 입력> ");
	scanf("%d", &num);
	do
	{
		if (num % i == 0)
			printf("%d ", i);
		i++;
	} while (i<=num);*/
  return 0;
}
