#include<stdio.h>
#include<time.h>
#include<stdlib.h>
#pragma warning(disable:4996)

int main()
{
  /*int x, y;
	printf("좌표(x y)");
	scanf("%d %d", &x, &y);
	if (x > 0 && y > 0)
		printf("1사분면");
	else if (x > 0 && y < 0)
		printf("4사분면");
	else if (x < 0 && y>0)
		printf("2사분면");
	else if (x < 0 && y < 0)
		printf("3사분면");*/
    
  /*char ch;	
	printf("문자를 입력하시오:");
	scanf("%c", &ch);
	switch (ch)
	{
	case 'R':
	case 'r':
		printf("Ractangle\n"); break;
	case 'T':
	case 't':
		printf("Triangle"); break;
	case 'C':
	case 'c':
		printf("Circle"); break;
	default:
		printf("Unknown");
	}*/
  return 0;
}
