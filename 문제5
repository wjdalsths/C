#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)

int main()
{
	/*int num, sum=0;
	printf("숫자 입력>");
	scanf("%d", &num);
	while (num)
	{
		sum += num % 10;
		num = num / 10;
	}
	printf("%d", sum);*/

	/*int num, sum = 0,cnt=0;
	printf("숫자 입력>");
	scanf("%d", &num);
	if (!num)
		cnt = 1;
	while (num)
	{
		num = num / 10;
		cnt++;
	}
	printf("%d자리수", cnt);*/
	
	/*int num,cnt=0,sum=0,a=0;
	printf("iput number>> ");
	scanf("%d", &num);
	while (num!=0)
	{
		cnt++;
		sum += num;
		scanf("%d", &num);
	}
	a = sum / cnt;
	printf("입력된 자료 개수: %d", cnt);
	printf("입력된 자료 합계: %d", sum);
	printf("입력된 자료 평균: %.2f", a);*/

	/*int num;
	int sum = 0, cnt = 0;
	double avg;
	while (1)
	{printf("iput number>> ");
	scanf("%d", &num);
	if (num==0)
		break;
	sum += num;
	cnt++;
	
	}
	avg = sum / cnt;
	printf("입력된 자료 개수: %d\n", cnt);
	printf("입력된 자료 합계: %d\n", sum);
	printf("입력된 자료 평균: %.2f", avg);*/
	
	/*int num, sum=0, avg,cnt=0;
	scanf("%d", &num);
	while (num!= 0)
	{
		sum += num;
		cnt++;
		scanf("%d", &num);
	}
	avg = sum / num;
	printf("홀수 합 %d", sum);
	printf("홀수 평균 %.2f", avg);*/
	return 0;
}
