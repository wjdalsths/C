#include<stdio.h>
#pragma warning(disable:4996)
int main()
{
  /*char  c='a';
	int n = 1;
	for (int i = 0; i < 4; i++)
	{
		for (int j = 0; j <= i; j++)
		{
			printf("%-3c", c++);
			
		}
		for (int j = 0; j < 4-i; j++)
			printf("%-3d", n++);
	printf("\n");
	}*/
  
  //피라미드
	/*int floor;
		printf("쌓을 층 수? ");
		scanf("%d", &floor);
		for (int i = 0; i < floor; i++)
		{
			for (int j = i; j < floor - 1; j++)
			{
				printf(" ");
			}
			for (int k = 0; k < i * 2 + 1; k++)
			{
				printf("*");
			}
			printf("\n");
	  }*/
  return 0;
}
