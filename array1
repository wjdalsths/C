#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#pragma warning(disable:4996)

int main()
{
	//문제1
	/*int score[] = { 100, 88, 100, 100, 90 };
	int s = 0,size=sizeof(score)/sizeof(int);
	float a;
	for (int i = 0; i < size; i++)
	{
		s += score[i];
	}
	a = s / (float)size;
	printf("총점 = %d\n", s);
	printf("평균 = %.2f", a);*/
	
	//문제2 10개 정수 입력받아 큰수 작은수 구하기
	/*int score[10];
	for (int i = 0; i < 10; i++)
	{
		printf("%d번 ", i+1);
		scanf("%d", &score[i]);
	}
	int max=score[0], min=score[0];
	for (int i = 0; i < 10; i++)
	{
		if (max < score[i])
			max = score[i];
		if (min > score[i])
			min = score[i];
	}
	printf("큰수는 = %d\n작은수는 = %d", max,min);*/

	//문제3   열개의 정수 입력받아 짝수중 가장 큰수,홀수중 가장 작은수 출력
	/*int score[10];
	for (int i = 0; i < 10; i++)
	{
		printf("%d번째 정수 입력 > ",i+1);
		scanf("%d", &score[i]);
	}
	int max = -10000, min = 10000;
	int size = sizeof(score) / sizeof(int);
	for (int i = 0; i < size; i++)
	{
		if (score[i] % 2 == 0) {
			if (max < score[i])
				max = score[i];
		}
		else {
			if (min > score[i]);
				min = score[i];
		}
	}
	printf("짝수중 큰수= %d\n홀수중 작은수= %d", max, min);*/
	return 0;
} 
