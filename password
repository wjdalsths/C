#include<stdio.h>
#pragma warning(disable:4996)
int main()
{
  //패스워드 일치하면 성공 ,아니면 다시작성	
	//const변경불가변수
	/*const int pw = 1234;
	int code;
	do
	{
		scanf("%d", &code);
		
	} while (pw != code);
		printf("성공");*/
  return 0;
}
