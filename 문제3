#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#pragma warning(disable:4996)
void name1(int n,char*name) {
	for (int i = 0; i < n; i++) {
		printf("%s", name);
		printf("\n");
	}
}
int main()
{
  /*char name[50] = { "Son jeongmin" };
	printf("%s\n", name);
	int n = strlen(name);
	printf("문자열 길이: %d\n", n);
	name1(n,&name);*/
  return 0;
}
