#include<stdio.h>
#pragma warning(disable:4996)
int main()
{
  //구구단
	/*int i, j;
	for ( i = 1; i <= 9; i++)
	{
		for (j = 1; j <= 9; j++)
		{
			printf("%d * %d = %-3d  ",j,i, i * j);
		}
		printf("\n");
	}
	int i, j;
	for (i = 1; i <= 9; i++)
	{
		printf(" %d단\n", i);
		for (j = 1; j <= 9; j++)
			printf("%3d\n", i * j);
		printf("\n");
	}
  return 0;
}
