#include<stdio.h>
#pragma warning(disable:4996)
int main()
{
  /*int score, result;
	printf("점수입력 >");
	scanf("%d", &score);
	result = score / 10;
	switch (result)
	{
		case 10:
		case 9: printf("수"); break;
		case 8:	printf("우"); break;
		case 7:	printf("미"); break;
		case 6:	printf("양"); break;
		default: printf("가");
	}*/
  
  /*int a, b, d;
	char c;
	printf("수식을 입력하시오(예 2 + 5)");
	scanf("%d %c %d", &a, &c, &b);
	switch (c)
	{
	case '+': printf("%d %c %d = %d", a, c, b, a + b);
	case '-': printf("%d %c %d = %d", a, c, b, a - b);
	case '*': printf("%d %c %d = %d", a, c, b, a * b);
	case '/': printf("%d %c %d = %d", a, c, b, a / b);
	default: printf("%d %c %d = %d", a, c, b, a % b);
	}*/
  
  /*int a;
	scanf("%d", &a);
	switch (a)
	{
	case 1:printf("삽입을 선택하셨습니다."); break;
	case 2:printf("수정을 선택하셨습니다."); break;
	case 3:printf("삭제을 선택하셨습니다."); break;
	default:printf("숫자를 입력하세요");
	}*/
  return 0;
}
