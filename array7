#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<time.h>
#pragma warning(disable:4996)
int main()
{
	//2차원 배열 더블정렬로 알파벳 숫서대로 배열
	/*char arr[5][50];
	for (int i = 0; i < 5; i++)
		scanf("%s", arr[i]);
	char temp[10];
	int i, j;
	for (i = 0; i < 5; i++)
	{
		for (j = 0; j < 4; j++)
		{
			if (strcmp(arr[j], arr[j + 1]) > 0) {
				strcpy(temp, arr[j]);
				strcpy(arr[j], arr[j + 1]);
				strcpy(arr[j + 1], temp);
			}
		}
	}
	for (int i = 0; i < 5; i++)
		printf("%s\n", arr[i]);*/
    	return 0;
}
